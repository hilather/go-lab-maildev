# LabMail 1.0.0-rc.1 Release Notes

Release date: 2026-08-18
Previous release: none (first public candidate; empty-tree baseline)
Application version: 1.0.0-rc.1
Configuration versions: labmail.dev/v1alpha1
REST versions: /v1
MCP protocol versions: 2026-07-28
Container digest: unpublished (Dockerfile is in-tree; `ghcr.io/hilather/labmail` is not pushed from this tag)

These notes are curated. They are not a commit list.

LabMail is a **receive-only lab sink**. It is **not** a public MTA and does **not** claim RFC 5321/5322 completeness. rc.1 is tagged only on a green CI SHA via the Release `tag-gate`.

Residual: [docs/known-limitations.md](https://github.com/hilather/go-lab-maildev/blob/v1.0.0-rc.1/docs/known-limitations.md).

## Highlights

LabMail captures RFC 5321 SMTP from systems under test and exposes every accepted message over native REST `/v1`, Streamable HTTP MCP `/mcp`, maildev `/email` compat, and an embedded inbox SPA on `:1080`. It never opens an outbound SMTP session and never implements relay.

First-candidate decisions frozen here:

- License **Apache-2.0**
- Module **`github.com/hilather/go-lab-maildev`**
- Toolchain **Go 1.26**
- Binary **`labmail`**. Image name **`ghcr.io/hilather/labmail`** (Dockerfile in-tree; this tag does not publish a digest).
- YAML **`labmail.dev/v1alpha1`**, kind **`LabMail`**
- MCP **2026-07-28** with TacLab `allowLegacyClients` knob (D17)
- Receive-only is structural (reserved YAML keys, no `Dial` in production SMTP/store/app, `POST /email/:id/relay` is 403)
- Inbox UI ships in this candidate (**Q2**: GA / 1.0 is not done without the inbox; the SPA is on this branch)
- Swap-release labinfo id stays **`maildev`** (D15 / Q1)

This file is the 1.0.0-rc.1 release notes. Design PRs 1–14 are on `main` (SMTP AUTH/STARTTLS, OBS, hardened image files, swap overlay examples, inbox UI). This rc is still a lab-sink candidate, not public-MTA GA.

## Added

- Single static binary `labmail`: `serve`, `validate`, `canonicalize`, `healthcheck`, `version`, `help`, `mcp-stdio`.
- In-tree SMTP sink (`internal/smtp/{codec,server}`): greeting, HELO/EHLO, MAIL/RCPT/DATA/RSET/NOOP/QUIT/HELP, VRFY=252, EXPN=502, SIZE/8BITMIME/SMTPUTF8/ENHANCEDSTATUSCODES, optional AUTH PLAIN/LOGIN and STARTTLS, session and in-flight DATA caps. Interop: `net/smtp.SendMail` against localhost. Implicit SMTPS remains a validate reject.
- Fail-closed `labmail.dev/v1alpha1` YAML: `KnownFields(true)`, reserved relay-key reject, default materialization, canonical `sha256:` revision.
- Bounded memory inbox: Crockford ULID ids, MIME via `internal/mimeparse`, stacked resident caps, `fullPolicy: reject` → SMTP 452, Wait + timeout, Wipe epoch (stale Insert → 451). Malformed MIME is still stored.
- HTTP-less `internal/app.Service`: atomic snapshot, plan/apply, reset that rereads YAML and wipes the inbox.
- Native REST `/v1`: problem+json, HMAC list cursors, wait/extract (frozen RE2), preview CSP + `cid:` → `data:`, plan/apply, health/ready, generated OpenAPI and capability manifest.
- Streamable HTTP MCP at `POST /mcp`: official SDK `v1.7.0`, protocol `2026-07-28`, `mail_*` tools, `labmail://` resources, `make test-parity`.
- maildev 2.2.1 compat: `GET /email` JSON array (bodies omitted), get/delete/html/attachment, `/healthz`, redacted `/config` (`receiveOnly: true`). Relay is always 403.
- Lab static bearer + HTTP Basic onto the same principal. MCP is bearer-only. `TestMaildevScenarioCompat` (SendMail + 401 + Basic subject). Session cookie `labmail_session` + `X-LabMail-CSRF`.
- Embedded inbox SPA (React/TS + Vite, Node **22.14.0**): login, list, sandboxed HTML preview, status, scoped audit, gated reset. No Relay/send/compose. First-party UI (not MailDev’s Angular app).
- Observability: slog JSON events, hand-rolled OpenMetrics (`spec.observability.metrics.listen`, default `127.0.0.1:9090`), catalog [api/metrics/v1alpha1.json](https://github.com/hilather/go-lab-maildev/blob/v1.0.0-rc.1/api/metrics/v1alpha1.json).
- Hardened image files: `Dockerfile` (scratch, UID 65532), `examples/compose.smoke.yaml`, `scripts/test-container.sh`. Image is not published from this tag.
- Swap overlay examples: `examples/labmail.yaml`, MCPJungle JSON, labinfo `maildev` catalog id. Compose/image pin in mcp-integration-lab is a follow-up.
- Side-by-side probe (`internal/compatcheck`, `TestSideBySideMaildev221`) against shipped `labmail serve` and live `maildev/maildev:2.2.1` when Docker is available.
- GA-001: committed fuzz corpora, `internal/perf` soak (accept N, wait, wipe), this notes file, [docs/known-limitations.md](https://github.com/hilather/go-lab-maildev/blob/v1.0.0-rc.1/docs/known-limitations.md), changelog check, Release `tag-gate`.

## Changed

- None. First public candidate; there is no previous tag to change.

## Fixed

- None relative to a previous tag. Correctness relative to the design pack is the implementation itself.
- MCP inbox `resources/updated` is fan-out via the official SDK on both Streamable HTTP and `mcp-stdio` (URI-only `labmail://messages`).
- Reset preflights store options and installs new caps under one lock, so a failed reset cannot empty the inbox under the old snapshot.

## Removed or deprecated

- None. First public candidate. No deprecations. Relay is refused, not removed from a prior LabMail release.

## Security

- Receive-only: no SMTP client; reserved YAML keys; import-boundary tests; `POST /email/:id/relay` is 403 `receive_only`.
- Default management auth is `bearer_and_basic`. Unauthenticated `GET /email` is 401. MCP is bearer-only. No OAuth PRM.
- `X-Forwarded-For` is not trusted. Present non-loopback `Origin` is default-deny; missing Origin is allowed.
- HTML preview: iframe `sandbox` without scripts/same-origin; CSP `default-src 'none'; img-src data:; style-src 'unsafe-inline'`; `cid:` inlined as `data:`; download route is `Content-Disposition: attachment`.
- Secrets are file references, never inline (except optional `LABMAIL_ALLOW_ENV_SECRETS=1` dev). Export/audit/logs redact token values and SMTP passwords.
- `make security-scan` runs govulncheck. Container posture is UID 65532, scratch, no caps (`Dockerfile` in-tree; digest unpublished).

## SMTP behavior

- Banner: `220 <hostname> LabMail ready`. The banner does not contain “maildev”.
- Commands and limits: [docs/02-smtp-semantics.md](https://github.com/hilather/go-lab-maildev/blob/v1.0.0-rc.1/docs/02-smtp-semantics.md). AUTH PLAIN/LOGIN and STARTTLS (`starttls`, optional or required) are implemented. When STARTTLS is required, AUTH is withheld on cleartext.
- Implicit SMTPS remains a validate reject (1.1).
- Successful DATA replies `250 2.0.0 Queued as <ULID>`. Empty `MAIL FROM:<>` is accepted.
- This is a lab capture sink. It does not resolve MX, open outbound TCP, rewrite recipients, or generate DSNs.

## Inbox UI

- Embedded at `/` when `spec.ui.enabled` is true (default). `enabled: false` 404s `/` and keeps REST/MCP.
- Login via bearer or Basic → `POST /v1/session`. Cookie is REST-only.
- Pages: inbox list, message view (text / sandboxed HTML preview / headers / raw / attachments), status, scoped audit, gated reset.
- Live update: `EventSource` `GET /v1/events/stream` with a 3s poll fallback. No maildev WebSocket.
- No Relay, send, outgoing settings, or compose. Required for GA / 1.0 (Q2); present in this candidate.

## REST API

- stdlib `net/http` adapter on the management listener (default `:1080`). OpenAPI: [api/openapi/v1.json](https://github.com/hilather/go-lab-maildev/blob/v1.0.0-rc.1/api/openapi/v1.json). Capability table: [api/capabilities/v1.json](https://github.com/hilather/go-lab-maildev/blob/v1.0.0-rc.1/api/capabilities/v1.json).
- Errors are `application/problem+json`. Domain `code` is first-class (`cursor_stale`, `store_over_new_cap`, `receive_only`, …).
- Native `GET /v1/messages/{id}` defaults `markRead=false`. Compat `GET /email/:id` marks read (maildev).
- Health live/ready skip auth. Ready requires SMTP bound and (management bound or explicitly off).

## MCP API and protocol compatibility

- Pinned protocol **2026-07-28**. Manifest: [api/mcp/v1.json](https://github.com/hilather/go-lab-maildev/blob/v1.0.0-rc.1/api/mcp/v1.json).
- Official SDK `github.com/modelcontextprotocol/go-sdk v1.7.0`. Streamable HTTP at `/mcp` is stateless.
- `spec.management.mcp.allowLegacyClients` default **false** (lab overlay sets true). `subscriptions/listen` stays 2026-07-28 even when the pin is relaxed.
- Tools and `labmail://` resources come from the shared registry. Native `mail_message_get` defaults `markRead=false`.
- Stdio is a developer adapter (`labmail mcp-stdio --token-file`) and is not an image entrypoint.

## Configuration and schema

- Strict v1alpha1 decode in `internal/config`. Published schema: [api/jsonschema/labmail.dev.v1alpha1.json](https://github.com/hilather/go-lab-maildev/blob/v1.0.0-rc.1/api/jsonschema/labmail.dev.v1alpha1.json).
- Unknown fields, YAML aliases, multi-doc streams, bare numbers, reserved relay keys, `maxMessageBytes: 0`, and `smtp.tls.mode: implicit` fail closed.
- Canonical export materializes defaults and drops comments. Revision is `sha256:` of canonical JSON. Inbox contents do not set `drifted`.
- Reset rereads the bootstrap path (never writes it) and wipes the inbox (only epoch bump).

## Deployment and operations

- `labmail serve --config PATH` compiles bootstrap and binds SMTP (default `:1025`) plus management (default `:1080`). `--management-listen=off` leaves management unbound. `SIGTERM` is graceful; `SIGUSR1` is unused.
- Image name `ghcr.io/hilather/labmail` (Dockerfile in-tree). This tag does not publish a digest. Host ports stay 1025 / 1080.
- Required CI jobs have no bypass. Tag path is the Release workflow `tag-gate` only.
- `make test-container` builds the image and checks non-root / read-only / no-caps when Docker is available. Soak: `go test ./internal/perf` (default N=8).

## Observability

- Frozen metric names and slog events: [docs/09-observability.md](https://github.com/hilather/go-lab-maildev/blob/v1.0.0-rc.1/docs/09-observability.md). Hand-rolled OpenMetrics on `spec.observability.metrics.listen` (default `127.0.0.1:9090`; empty disables).
- `GET /v1/health/live` and `GET /v1/health/ready` (compat `GET /healthz`) are implemented. Ready does not require MCP clients or a non-empty inbox.
- Do not put subjects or addresses in metric labels.

## Compatibility and migration

- First install only. There is no predecessor application tag.
- Pin config `apiVersion: labmail.dev/v1alpha1`. Unknown fields fail closed.
- MCP clients must speak `2026-07-28` unless `allowLegacyClients: true`.
- Rollback is process restart onto a previous desired-state pin. Captured mail is discarded on any restart.
- maildev 2.2.1 `/email` read/delete shape is best-effort; relay will never work. ULID ids, sha256 checksums, omitted list bodies, and no WebSocket are documented deltas.
- A later `v1.0.0` tag needs its own `docs/releases/v1.0.0.md` and a green `tag-gate`. **GA / 1.0 is not this rc.** Inbox UI, AUTH/STARTTLS, OBS, image files, and swap overlays are in this tree. Residual: no published image digest, no mcp-integration-lab compose pin, not a public MTA.

## This tree versus stacked siblings

Design PRs 1–14 are merged. What is **not** this tag:

| In this tree | Not this tag |
|---|---|
| SMTP-001a/b, STORE, STA, REST, MCP, compat, SEC, UI, OBS, DEP files, SWAP examples, GA, side-by-side probe | Published `ghcr.io/hilather/labmail` digest |
| First-party inbox SPA | MailDev Angular / WebSocket UI |
| Receive-only 403 relay | Outbound SMTP / maildev outgoing |
| Overlay examples in this repo | mcp-integration-lab compose/image pin |

## Known limitations

See the durable list: [docs/known-limitations.md](https://github.com/hilather/go-lab-maildev/blob/v1.0.0-rc.1/docs/known-limitations.md).

- Not a complete / public MTA. No DSN, CHUNKING, PIPELINING, Sieve, per-recipient quotas, or greylisting.
- MIME parse of pathological messages may yield empty text/html with `parseWarning`; raw is still stored.
- Default `maxMessageBytes` is 10 MiB (maildev implicit ~50 MiB).
- No implicit SMTPS in 1.0. AUTH PLAIN/LOGIN and STARTTLS are implemented; implicit TLS stays reject.
- Compat `/email` ULID ids; list omits bodies; sha256 checksum; no WebSocket; relay is 403.
- Worst-case RSS ≈ 384 MiB (256 MiB stored + 64 MiB in-flight + ~64 MiB slack). Single replica.
- MCP has no OAuth PRM. Catalog id stays `maildev` during the swap release.
- No published image digest, SBOM, or provenance on this candidate. Default soak is 8 messages.

## Complete functionality-difference review

Every public surface is **added** relative to the empty tree. Reviewed and represented above:

- [x] OpenAPI
- [x] MCP capability manifest and tool/resource schemas
- [x] Capability table
- [x] Configuration schema and defaults
- [x] CLI flags and environment variables
- [x] SMTP command support (001a + AUTH PLAIN/LOGIN + STARTTLS; implicit SMTPS still absent)
- [x] Error code catalog
- [x] Metrics and labels catalog
- [x] Metrics listener / OpenMetrics bind
- [x] Deployment contract (CLI, ports, healthcheck plane)
- [x] Deployment files (Dockerfile, compose smoke); image digest not published
- [x] Dependencies and SBOM (module pins; tag-time SBOM remains)

## CI and release evidence

- [ ] All required CI passed on the exact tag commit.
- [x] No required check was bypassed.
- [x] Any CI failure encountered was fixed and hardened.
- [ ] Container digest maps to the tag commit. (No image in this tree.)
- [ ] Security scans were reviewed. (`govulncheck` is the local command; tag-time SBOM/provenance remain.)
- [ ] Upgrade and rollback were tested. (N/A predecessor. Restart discards the inbox.)

Unchecked boxes: required GitHub CI green-on-tag is enforced by `tag-gate` after this tag is pushed; no container digest is published from this candidate.
